# اختبار VOODO ERP — دليل عملي

فيه نوعين بجّات، وكل نوع بيتصاد بطريقة مختلفة. الخلط بينهم هو اللي خلى
بجّات زي "استيراد الإكسيل بيروح لفرع تاني" تعدي من أكتر من مراجعة كود.

---

## 1️⃣ الفحص الأوتوماتيكي — قبل أي `git push`

```bash
python audit.py
```

بيمسك في أقل من ثانية نوع البجّات اللي **المراجعة البشرية بتفوّتها**، لأن
كل ملف لوحده بيبان سليم والمشكلة في التعارض بين الملفات:

| الفحص | البج الحقيقي اللي اتكتب عشانه |
|---|---|
| IDs مكررة | ملاحظات أمر الشراء كانت بتتلغي (نفس الـ id في مودالين) |
| IDs مكررة | فلتر فرع تقرير المخزون مكانش شغال خالص |
| أكتر من فلتر فرع في صفحة | استيراد الإكسيل راح لفرع غير اللي على الشاشة |
| `onclick` يتيم | زرار بينادي دالة اتمسحت → بيقع صامت |
| عنصر مفقود | JS بيدوّر على `id` مش موجود → الفيتشر بيفشل من غير رسالة خطأ |

⚠️ **الأداة دي بتفحص التناسق البنيوي بس.** مش بتعرف إن الحسبة غلط أو إن
الفيتشر منطقه ملخبط — ده شغل القسم اللي تحت.

---

## 2️⃣ الاختبار اليدوي — اللي الأتمتة مش هتشوفه

### القاعدة الذهبية بعد أي تعديل
**جرّب الفيتشر بحسابين مختلفين على الأقل**: المالك، وكاشير فرع.
معظم بجّات السيستم دي كانت بتظهر في حساب واحد بس.

### سيناريو الفروع (أخطر منطقة — 3 بجّات طلعت منها)
> أي صفحة فيها فلتر فرع: **غيّر الفلتر لفرع تاني، وبعدين اعمل عملية كتابة**
> (إضافة/تعديل/حذف/استيراد) — وتأكد إن اللي اتغيّر هو الفرع اللي على الشاشة
> مش فرعك الافتراضي.

- [ ] المخزون: غيّر الفرع → استورد إكسيل → المنتجات ظهرت في نفس الفرع؟
- [ ] المخزون: غيّر الفرع → عدّل كمية صنف → اتغيّرت في الفرع الصح؟
- [ ] المخزون: غيّر الفرع → صدّر إكسيل → الملف فيه بيانات الفرع المعروض؟
- [ ] التقارير: غيّر فلتر الفرع في كل تبويب → الأرقام بتتغيّر فعلاً؟
- [ ] بحساب كاشير: فلتر الفرع مقفول على فرعه ومش بيقدر يغيّره؟

### الفلاتر عموماً
- [ ] كل صفحة فيها **فلتر فرع واحد بس** (فلتر الهيدر بيختفي في الصفحات
      اللي ليها فلتر خاص)
- [ ] "🌎 كل الفروع" شغال في المخزون والتقارير والمبيعات

### الحفظ والاسترجاع (بج ملاحظات أمر الشراء كان هنا)
> أي فورم: **اكتب في كل حقل، احفظ، اقفل، افتح تاني** — كل قيمة رجعت زي
> ما كتبتها؟ الحقول اللي بتتنسى بصمت هي الأخطر.

- [ ] أمر شراء: املا الملاحظات → احفظ → افتح تاني → الملاحظات موجودة؟
- [ ] منتج جديد: كل الحقول (تكلفة/سعر قبل/بعد/فئة/مجموعة) اترجعت صح؟
- [ ] مستخدم جديد: الصلاحيات اللي اخترتها اترجعت زي ما هي؟

### الصلاحيات
- [ ] اعمل مستخدم أدمن بصلاحيات محدودة → سجّل دخول بيه → التابات المقفولة
      مش ظاهرة **ومش بتفتح حتى لو كتبت لينكها**؟
- [ ] كاشير/مدير فرع: صلاحياتهم ثابتة ومش قابلة للتعديل؟
- [ ] تابات المالك (الإعدادات/المستخدمين/الترحيل) مقفولة على أي حساب تاني؟
- [ ] احذف مستخدم → جرّب تسجّل دخول بيه → مرفوض؟

### سجل التغييرات
- [ ] بعد كل عملية فوق: افتح سجل التغييرات → العملية اتسجلت؟
- [ ] اضغط 🔍 التفاصيل → بيوريك القيم قبل وبعد؟

### المودالات — «مش مخفي» مش معناها «ظاهر»
> أي مربع تأكيد أو إدخال بيتفتح **من جوه مودال تاني**: اتأكد إنك شايفه فعلاً
> وإنك تقدر تدوس عليه، مش إن الكود بيناديه وخلاص.

كل `.modal-overlay` ليها نفس `z-index:1000`، فاللي أبعد في الـ DOM بيتحط فوق.
`genericConfirmModal` و`genericPromptModal` معرّفين بدري (سطر ~2255)، فأي مودال
بعدهم كان بيدفنهم: السؤال بيتنده، والكليك بيروح للمودال اللي فوق، وشكل الحاجة
إنها مش بتشتغل. اتصلح بـ `z-index:1100` ليهم (`src/css/styles.css`) — بس أي
مودال جديد بنفس النمط ممكن يرجّع المشكلة.

- [ ] السؤال ظهر قدامك فعلاً (مش لقيته بالصدفة في الـ DevTools)
- [ ] زرار «موافق» بيستجيب لأول كليك
- [ ] بعد ما تلغي، الشاشة اللي كنت فيها رجعت زي ما هي

⚠️ **في الاختبار الأوتوماتيكي**: `classList.contains('hidden')` بيقول إن العنصر
في الـ DOM، **مش** إنه ظاهر أو قابل للضغط. استخدم
`document.elementFromPoint(x, y)` وتأكد إن العنصر اللي راجع هو المودال المطلوب
وإن الزرار هو اللي بيتلمس عند إحداثياته. الفحص الغلط ده خلّى بج يتشحن مرتين
(زرار إلغاء الفاتورة المعتمدة، وسؤال إضافة صنف أثناء الجرد).

### الكاشير (بعد أي تعديل يمس البيع)
- [ ] بيع فاتورة كاملة (بحث → سلة → دفع → طباعة)
- [ ] الفاتورة اتسجلت على الفرع الصح؟ والمخزون نقص من الفرع الصح؟
- [ ] فتح وقفل وردية بعدّ الدرج

---

## 3️⃣ الأوديت الشهري — ٢٠ دقيقة

مرة كل شهر. مش إعادة لكل حاجة فوق — دي الفحوصات اللي **بتكشف تلف صامت**
تراكم خلال الشهر. رتّبها من الأسرع للأبطأ.

### أ) الأوتوماتيكي — ثانية واحدة
```bash
python audit.py
```

### ب) الأرقام الثلاثة لازم تتطابق (كشفت ٣ بجّات)
افتح نفس الشهر في **٣ شاشات** وقارن **صافي الربح**:

| الشاشة | المكان |
|---|---|
| الداشبورد | الرئيسية |
| قائمة الدخل | المحاسبة ← قائمة الدخل |
| الملخص | المحاسبة ← الملخص |

**التلاتة لازم يقولوا نفس الرقم.** أي اختلاف = بج في واحد منهم، والأغلب
إن السبب مرتجع محسوب في اتجاه واحد بس.

### ج) مخزون سالب — مفيش صنف يقل عن صفر
في الكونسول (بحساب المالك):
```javascript
BRANCH_IDS.flatMap(b => getInv(b).filter(p => p.qty < 0).map(p => `${b}: ${p.name} = ${p.qty}`))
  .forEach(x => console.log('❌', x)) || console.log('✅ مفيش مخزون سالب');
```
أي نتيجة هنا معناها فيه مسار بيبيع أكتر من المتاح.

### د) النسخة الاحتياطية كاملة
```javascript
console.log('الفروع في النسخة:', Object.keys(_buildFullBackup().invByBranch));
```
لازم تشوف **كل** فروعك. فرع ناقص = نسخة احتياطية ناقصة (شوف
`_syncExtraBranchIds` — دي كانت بج حقيقي).
ولو كل حاجة تمام: **نزّل نسخة واحفظها بره الجهاز**.

### هـ) الصلاحيات لسه مُنفَّذة على السيرفر
سجّل دخول بحساب `testcashier` (فرع باكوس `b5`) في نافذة خفية، وشغّل
سكريبت التحقق — المفروض الكاشير مايقدرش يقرا مخزون أو مبيعات فرع تاني.
ده بيتأكد إن حد ما نشرش قواعد غلط بالغلط خلال الشهر.

### و) المستخدمون — نضافة
- شيل أي حساب لموظف مشي
- بصّ على "حسابات دخول محلية قديمة" — لازم تفضل فاضية

### ز) الشهر ده اتضاف إيه؟
لكل فيتشر جديد اتعمل، اسأل السؤال اللي كشف ٥ بجّات:
> **عملت الاتجاه المعاكس؟** (الحذف، الإرجاع، الخصم، الإلغاء)

---

## 4️⃣ لو لقيت بج

اسأل نفسك: **"هل الأداة كانت تقدر تمسكه؟"**
- لو أيوة → ضيف الفحص في `audit.py` عشان ميتكررش
- لو لأ → ضيف السيناريو في القايمة فوق

كده كل بج بيتصاد مرة واحدة بس.

---

## ⚠️ قبل نشر `firestore.rules`

```bash
test-rules.bat
```

بيشغّل `firestore.rules` الحقيقي على محاكي محلي و31 سيناريو عليه — منع
الاعتماد الذاتي، تثبيت الأسعار وقت الاعتماد، انتهاء صلاحية الاعتماد، عزل
مخزون الفروع، `pos_data/auth` مقفول، سجل التغييرات append-only. بيرجّع exit
code 1 لو أي واحد فشل.

مستحيل يلمس الإنتاج — بيشتغل على مشروع وهمي (`voodo-rules-test`) على جهازك.

**أي قاعدة جديدة تتضاف لـ `firestore.rules` → يتضاف ليها اختبار في
`rules-test/run-tests.js`.** نفس قاعدة `audit.py`: كل ثغرة تتصاد مرة واحدة بس.

الخطوات الكاملة (وترتيب النشر) في [`CLAUDE.md`](CLAUDE.md). **ممنوع** النشر
من غير ما الاختبارات تعدّي.
